language: android
jdk: oraclejdk8

script:
  - ./gradlew build -x lint

deploy:
  provider: releases
  api_key: $GITHUB_ACCESS_TOKEN
  file:
    - build/outputs/apk/app-debug.apk
  skip_cleanup: true
  on:
    tags: true
